name: Phase 1 - Watcher
on:
  workflow_dispatch:
  schedule:
    - cron: "0 */8 * * *"

jobs:
  watcher:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Phase 1 triggered."